# NON-VACUITY FIXTURE

Deliberately violating input for the `md↔gherkin` gate — see
`scripts/nonvacuity/bad-md-gherkin.mjs`. The feature file is real
(`scenario-binding.feature`, the same one `docs/crossvalidate.md` cites), so
only one thing is wrong: the scenario title below does not exist in it.

`scenario-binding.feature` · 'A scenario title that does not exist anywhere'
