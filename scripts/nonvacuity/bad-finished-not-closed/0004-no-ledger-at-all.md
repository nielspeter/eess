# Bug 0004: no ledger at all

## Status

- **State:** Draft — a control. It has NO task boxes, so it claims nothing and
  must not be reported as finished. Sabotage 3 (dropping the `boxes.length === 0`
  guard) survived the fixture until this record existed.

## Notes

Prose only. Nothing is owed here, and nothing is claimed.
