# Bug 0005: a state nobody can parse

## Status

- **State:** Marinated — a control. The value is outside the vocabulary, so
  `ledger/unknown-state` owns it and this rule must not offer a second opinion.
  Sabotage 5 (dropping the unreadable-state skip) survived until this record
  existed: every box below is ticked, so the rule WOULD fire if it guessed.

## Verification

- [x] done
- [x] also done
