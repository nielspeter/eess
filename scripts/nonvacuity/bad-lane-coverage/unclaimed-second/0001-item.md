# Fixture item — a second unclaimed lane

**State:** Draft — proves `findUncoveredLanes` reports every uncovered
directory, not just the first one it finds. A fixture with only one unclaimed
directory cannot catch a regression that silently caps the result at one hit
(verified by mutation: `if (violations.length > 0) continue` after the push in
`lane-coverage.mjs` passes the single-directory fixture unchanged).
