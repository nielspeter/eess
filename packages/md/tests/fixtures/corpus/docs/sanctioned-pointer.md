# Sanctioned illustrative pointer

Prose explaining pointer syntax with an example:

<!-- eess-exclude corpus/pointers-resolve: illustrative syntax example, not a live claim -->
An example pointer looks like `src/missing.ts:1` in prose.

Block form survives reflow (blank lines allowed inside the range):

<!-- eess-exclude-start corpus/pointers-resolve: block-form example -->

Another example: `src/missing.ts:2` after a blank line.

<!-- eess-exclude-end -->
