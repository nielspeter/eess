# Ambiguous site-absolute link

[decoy](/decoy/) — repo-root has a real `decoy/` directory; the intended
content-root page (`site/decoy/`) does not exist. The hint must name the
repo-root candidate explicitly, not report it as if it were the page the
author meant.
