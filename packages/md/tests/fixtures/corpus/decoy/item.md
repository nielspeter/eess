# A real directory at repo-root, unrelated to any site content

Exists to prove the directory-hint labels which candidate it names when a
site-absolute link has two — repo-root and content-root — and only one of
them happens to be real. Reviewed 2026-08-13: an unlabelled hint would have
named this directory even when the author's actual mistake was a missing
content-root page.
