# ADRs — index & tier taxonomy

This is the schema/index doc for the ADRs, not an ADR itself. It has no
`## Enforcement` table on purpose — it must be exempt from `adrEnforcement`.

| Tier | Meaning |
| ---- | ------- |
| 1    | static  |
