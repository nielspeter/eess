# ADR-1: Use PostgreSQL

## Status

Accepted

## Context

We need a relational database. See [the db config](../src/db.ts) at `src/db.ts:1`.

## Decision

Use PostgreSQL.

## Consequences

Operations must run a Postgres instance.
