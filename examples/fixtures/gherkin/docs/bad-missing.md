# Story: a stale citation

This doc is intentionally broken — it cites a scenario that was renamed or
deleted, to demonstrate the build failing. See
`features/job-management.feature` · 'Trigger job manually' for the flow.
